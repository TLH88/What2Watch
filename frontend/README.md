# Frontend Notes

The frontend is a Vite + React application for the What2Watch UI.

Most users should use the root project instructions in the repository `README.md`.

For frontend-only development:

```bash
npm ci
npm run dev
```

The app expects the backend API to be available under `/api`. In the Docker Compose setup, the frontend and backend are wired together automatically.
